package org.facundosaracho.starwarschallenge.model.domain;

public record Result(
        Properties properties,
        String _id,
        String description,
        String uid,
        int __v,
        String url
) {}
